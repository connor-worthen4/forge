# report phase (stub, tier-0 read-only)

Role: perform the read-only investigation for a tier-0 task and write the findings to report.md. No code changes, no branch, no PR.

This is a STUB. In normal operation the runner sends the prose in this file to
`claude -p` as the phase prompt; the real report prompt is separate work and will
replace this body. In stub mode the runner writes a placeholder report.md.

<!-- forge:stub-result {"status":"ok"} -->
