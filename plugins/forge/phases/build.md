# build phase (stub)

Role: implement the change on the feature branch and capture the diff (diff.patch).

This is a STUB. In normal operation the runner sends the prose in this file to
`claude -p` as the phase prompt; the real build prompt is separate work and will
replace this body. In stub mode the runner uses the canned result below.

<!-- forge:stub-result {"status":"ok"} -->
