# intake phase (stub)

Role: validate and normalize the task spec, confirm it is actionable, and create the run record.

This is a STUB. In normal operation the runner sends the prose in this file to
`claude -p` as the phase prompt; the real intake prompt is separate work and will
replace this body. In stub mode the runner uses the canned result below so the
pipeline is end-to-end testable before real prompts exist.

<!-- forge:stub-result {"status":"ok"} -->
