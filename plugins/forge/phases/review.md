# review phase (stub)

Role: independent review of the change against the acceptance criteria and constraints; record a verdict. Runs in an isolated reviewer context.

This is a STUB. In normal operation the runner sends the prose in this file to
`claude -p` as the phase prompt; the real review prompt is separate work and will
replace this body. In stub mode the runner uses the canned result below. To
exercise the review->build recovery loop in tests, set FORGE_STUB_STATUS_review=fail.

<!-- forge:stub-result {"status":"ok"} -->
